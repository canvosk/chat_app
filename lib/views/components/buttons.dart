import 'package:flutter/cupertino.dart';

BoxDecoration loginButton = const BoxDecoration(
  borderRadius: BorderRadius.all(Radius.circular(30)),
  color: Color(0xFF295BE0),
);

BoxDecoration profileButton = const BoxDecoration(
  borderRadius: BorderRadius.all(Radius.circular(30)),
  color: Color(0xFF295BE0),
);

const LinearGradient addButton = LinearGradient(
    colors: [Color(0xFF7349FE), Color(0xFF643FDB)],
    begin: Alignment(0.0, -1.0),
    end: Alignment(0.0, 1.0));

const Color deleteButton = Color(0xFFFE3577);
